*.o
*.d
*.elf
*.dol
*.map

# Local macOS metadata
.DS_Store

# Keep the portable developer scripts; ignore local build products, ROMs,
# emulator profiles, captures and logs under .dev.
.dev/*
!.dev/*.sh
!.dev/*.ps1
# Local agent config; keep the shared Wii64 skill.
CLAUDE.md
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/wii64-build-and-test/
.claude/skills/wii64-build-and-test/*
!.claude/skills/wii64-build-and-test/SKILL.md
.mcp.json
# codebase-memory's local index artifact -- regenerate with index_repository,
# no need to carry a 6MB binary graph.db in the repo.
.codebase-memory/
# Other AI coding assistants, in case any get used here later
.cursor/
.cursorrules
.cursorignore
.windsurf/
.windsurfrules
.aider*
.codeium/
.continue/

__pycache__/

# Baseline screenshots: large binary media, kept out of git as in WiiStation
baselines/media/
/libogc2/
/libfat/
